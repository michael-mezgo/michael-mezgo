baseURL = "https://michael-mezgo.github.io/michael-mezgo"
defaultContentLanguage = "en"
languageCode = "en"

title = "Michaels Hugo Site"

theme = "gokarna"

# Automatically generate robots.txt
enableRobotsTXT = true

[menu]
  [[menu.main]]
    # Unique identifier for a menu item
    identifier = "posts"

    url = "/posts/"
    
    # You can add extra information before the name (HTML format is supported), such as icons
    pre = "<span data-feather='book'></span>"

    # You can add extra information after the name (HTML format is supported), such as icons
    post = ""

    # Display name
    name = "Posts"

    # Weights are used to determine the ordering
    weight = 1

  [[menu.main]]
    identifier = "tags"
    name = "Tags"
    url = "/tags/"
    pre="<span data-feather='tag'></span>"
    weight = 2
    
  [[menu.main]]
    identifier = "github"
    url = "https://github.com/michael-mezgo/"
    weight = 3
    
    # We use feather-icons: https://feathericons.com/
    pre = "<span data-feather='github'></span>"

[params]
  # URL for the avatar on homepage
  avatarURL = "/images/avatar.webp"

  # Choose one of size-xs, size-s, size-m, size-l & size-xl. (Default: size-m)
  avatarSize = "size-m"

  # Description to display on homepage
  description = "Personal Website"

  # Accent color is displayed when you hover over <a> tags
  accentColor = "#FF4D4D"

  # Keywords relevant for SEO
  metaKeywords = ["blog", "personal", "michael-mezgo"]

  socialicons = [
    {name = "github", url = "https://github.com/michael-mezgo/"}
  ]

  # Footer text
  footer = "michael-mezgo"
